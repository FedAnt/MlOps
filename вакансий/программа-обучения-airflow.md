# Программа обучения Apache Airflow (лаборатория)

Документ фиксирует **маршрут изучения** Airflow в связке с общим планом [`план-практики-инфраструктура-senior-devops.md`](./план-практики-инфраструктура-senior-devops.md) (**Фаза 4**: MinIO, Airflow, dbt, ClickHouse, Trino).

**Артефакты в репозитории:** `platform-service/docs/airflow-baseline.md`, DAG `airflow/dags/`, Helm `helm/airflow/values-dev.yaml`, CI job `deploy_airflow_dev`.

---

## Журнал уроков

### Урок 1 (закрыт)

Смысл Airflow, обход UI (Graph/Grid, trigger), схема «DAG → scheduler → БД → UI», домашнее задание. В репозитории добавлены **`doc_md`** у DAG и задач — в UI откройте DAG → **Details** / документация.

### Урок 2 (текущий)

**Теория (кратко):**

- Scheduler создаёт **DAG Run** на границе **data interval**; у каждого run есть **`logical_date`** (раньше «execution date») — это «метка» интервала данных, а не обязательно момент старта часов.
- **`catchup=False`** — не создавать прошлые run за всё время с `start_date` до «сегодня»; для лаборатории это норма.
- **`schedule_interval="@daily"`** — суточный тик; смена расписания влияет на **следующие** run, не переписывает историю.

**Практика:**

1. Задеплойте обновлённый `raw_to_staging.py` в кластер (команды в `docs/airflow-baseline.md`, раздел про загрузку DAG через `exec` + `cat`).
2. В UI откройте **DAG → Details / Documentation** — должен отображаться новый **`doc_md`** (таблица `task_id` ↔ смысл).
3. На вкладке **Code** (если включена) или в репозитории сопоставьте `task_id` с узлами Graph.
4. В репозитории уже добавлена **`BashOperator`** `echo_lab_marker` — задеплойте DAG, сделайте trigger и найдите в логе на scheduler строку `lesson-2 marker` (команды в `docs/airflow-baseline.md`, раздел «Логи задачи в UI пустые»).

**Критерий конца урока 2:** можете объяснить, зачем нужен `logical_date`, и в кластере после доставки файла в UI виден обновлённый граф (узел **`echo_lab_marker`**) + в файле лога на scheduler есть строка **`lesson-2 marker`**.

Чеклист: закройте пункт «минимальная правка + доставка» в разделе 3 после шагов 1–4.

### Пауза (зафиксировано)

**Прервались до следующего дня.** При возобновлении открывайте этот файл с места **«Урок 2»** (доделать практику/чеклист, если не успели), затем переходите к **следующему разделу**.

**Следующий раздел — Урок 3 (запланировано):**

- Параметры DAG: **`params`**, шаблоны в полях операторов (основы Jinja в контексте Airflow).
- **Variables** в UI / CLI: префиксы bucket, окружение; без секретов в коде.
- **Ретраи** на шагах с внешними системами (`retries`, `retry_delay`, когда не завышать).
- Задел под **Connection к MinIO** (тип `aws`/`amazon`, `endpoint_url` на сервис в кластере) — согласовано с разделом **5** этой программы и `docs/airflow-baseline.md`.

---

## 1. Смысл продукта (0.5–1 день)

| Тема | Содержание |
| --- | --- |
| Роль оркестратора | Airflow планирует и запускает **задачи** с **зависимостями**, **повторами** и **историей**; сам по себе не заменяет хранилище и «не переносит данные магически». |
| Базовые сущности | DAG, Task, Operator, Task Instance, DAG Run, Scheduler, метаданные (PostgreSQL), Web UI. |
| Отличие от cron | Зависимости между шагами, backfill (идея), мониторинг, единая точка для data-пайплайнов. |

**Критерий:** одной фразой объясняете, чем DAG отличается от одного длинного shell-скрипта по расписанию.

---

## 2. Как это работает (1–2 дня)

| Тема | Содержание |
| --- | --- |
| Парсинг DAG | Scheduler периодически читает файлы из каталога DAG, строит граф. |
| Планирование | Расписание / триггеры → решение о новом **DAG Run**. |
| Очередь и зависимости | Task Instance ждёт успешного upstream. |
| Исполнение | В лаборатории — **LocalExecutor** (упрощённая модель относительно Celery/Kubernetes Executor). |
| UI | Отображение состояния и логов из метаданных. |

**Критерий:** схема «файл DAG → scheduler → БД → исполнение → UI» без шпаргалки.

---

## 3. Пользователь DAG-разработчика (2–4 дня)

| Тема | Содержание |
| --- | --- |
| DAG как код | `DAG()`, `task_id`, связи `>>` / `set_upstream`. |
| Операторы | `BashOperator`, `PythonOperator`, `EmptyOperator`; концепт Sensor / deferrable. |
| Время и идемпотентность | `logical_date`, data interval; задачи должны быть безопасны при повторном запуске. |
| Connections / Variables | Учётки MinIO/S3, URL сервисов; секреты не в коде DAG. |
| XCom | Обмен небольшими метаданными между задачами; не таскать большие данные через XCom. |
| Отладка | `airflow dags list`, Import Errors в UI, логи задачи в UI. |
| Пауза и фильтры UI | `unpause`, URL без лишних query-параметров (см. baseline). |

**Критерий:** изменили DAG в репозитории, доставили в кластер по runbook, видите результат в UI.

---

## 4. Администратор платформы (3–5 дней, частично параллельно с п. 3)

| Тема | Содержание |
| --- | --- |
| Развёртывание | Helm release, values, namespace, Ingress (Traefik), `AIRFLOW__WEBSERVER__BASE_URL` **без** завершающего `/`. |
| Компоненты | webserver, scheduler, triggerer (если включён), PostgreSQL, том DAG / при переходе — git-sync. |
| Секреты | `FERNET_KEY`, `WEBSERVER_SECRET_KEY` для стабильности сессий и предсказуемости. |
| Ресурсы и сбои | requests/limits, OOM, `kubectl describe pod`, Events, логи webserver. |
| Операции | миграции БД, пользователи UI, backup метаданных (концепт RTO/RPO для лаборатории). |

**Критерий:** после `helm upgrade --wait` знаете, куда смотреть при зависании (поды, логи, события).

---

## 5. Данные и MinIO (1–2 дня)

| Тема | Содержание |
| --- | --- |
| Connection | S3-совместимый endpoint MinIO в кластере, connection в Airflow. |
| Паттерн | проверка зоны `raw` → шаг обработки → запись в `staging` (в лаборатории — упрощённо). |
| Граница ответственности | Airflow **оркестрирует**; тяжёлая SQL/витрины часто остаются за dbt / движками — вы позже свяжете это в разделе 6. |

**Критерий:** одна задача в DAG проверяет наличие объекта в bucket или читает метаданные без «тяжёлого» ETL.

---

## 6. Сквозной мини-проект (2–3 дня) — **точка возврата после других продуктов**

Здесь закрепляется связка **оркестратор ↔ остальной стек Фазы 4**. Если сначала уходите на изучение **dbt / ClickHouse / Trino**, после них **возобновляйте с этого раздела** (с п. **6.1** чеклиста ниже).

| Тема | Содержание |
| --- | --- |
| Расписание | DAG с `schedule` или понятным триггером для лаборатории. |
| Документация | `doc_md` у DAG/задач. |
| Интеграция | вызов dbt (`dbt run` / job), запрос к Trino, загрузка в ClickHouse — по мере появления этих компонентов в кластере. |
| Надёзность | ретраи на внешних шагах, осмысленные `retries`/`retry_delay`. |
| Runbook | для себя: «webserver не отвечает», «scheduler не видит DAG», «падение задачи с MinIO». |

**Критерий:** один DAG описывает кусок пути данных через уже развёрнутые сервисы; есть короткий runbook.

---

## Чеклист прогресса

Отмечайте по мере выполнения (`[x]` / `[ ]`).

### Раздел 1. Смысл

- [x] Прочитано введение и критерий раздела 1; можете устно объяснить роль Airflow.

### Раздел 2. Устройство

- [x] Понятен цикл: парсинг → run → task instance → исполнение → запись в БД → UI.
- [x] Просмотрен один DAG Run в UI (Grid/Graph) от начала до конца для тестового запуска.

### Раздел 3. Пользователь DAG

- [x] Открыт UI по стабильному URL из baseline (Ingress / hosts).
- [x] Найден DAG `raw_to_staging_minimal` (или актуальный id), снята пауза при необходимости.
- [x] Выполнен ручной trigger; открыты логи хотя бы одной задачи.
- [x] Внесена **минимальная** правка в DAG (новая задача или изменение `doc_md`), доставка в кластер по `airflow-baseline.md`.
- [x] Проверен раздел **DAG Import Errors** (пусто или исправлены ошибки).

### Раздел 4. Администратор

- [ ] Известны команды: `kubectl get pods`, `get ingress`, `get events`, логи `deploy/airflow-webserver`.
- [ ] Выполнен или осознан `helm upgrade` с `values-dev.yaml`; понятен смысл `--wait` и таймаута.
- [ ] Зафиксировано для себя: где задаётся `BASE_URL`, почему без trailing slash.
- [ ] (Рекомендуется) заданы стабильные `AIRFLOW_FERNET_KEY` и `AIRFLOW_WEBSERVER_SECRET_KEY` в CI/CD variables по baseline.

### Раздел 5. Данные / MinIO

- [ ] В Airflow создана Connection к MinIO (или запланирована и задокументирована заглушка с понятным следующим шагом).
- [ ] Одна задача DAG обращается к object storage (list head object / проверка префикса) без дублирования секретов в Git.

### Раздел 6. Сквозной мини-проект (**возврат после dbt / ClickHouse / Trino**)

- [ ] **6.1** — DAG с расписанием и `doc_md`.
- [ ] **6.2** — цепочка из ≥3 задач с явными зависимостями.
- [ ] **6.3** — параметры через `params` или Variables для префиксов/имён bucket.
- [ ] **6.4** — осмысленные ретраи на шаге, зависящем от внешней системы.
- [ ] **6.5** — короткий runbook (1 страница) в `docs/` репозитория или в этом файле в конце как приложение.

---

## Возврат после изучения других продуктов из списка Фазы 4

**Список продуктов Фазы 4** (помимо уже поднятого MinIO и Airflow): **dbt**, **ClickHouse**, **Trino** — см. практику в [`план-практики-инфраструктура-senior-devops.md`](./план-практики-инфраструктура-senior-devops.md), раздел «Фаза 4».

**На какой шаг вернуться:** к **разделу 6** этой программы и чеклисту — с пункта **6.1** («Сквозной мини-проект»). Имеется в виду: не перечитывать с нуля разделы 1–5, а **добавить в DAG оркестрацию уже освоенных инструментов** (вызов dbt, шаг с Trino, публикация в ClickHouse и т.д.) и довести **6.1–6.5** до выполнения.

Если раздел 6 **ещё не начинали** до ухода на dbt/CH/Trino — всё равно **вход в Airflow после них** с пункта **6.1**; разделы 1–5 при необходимости используйте как справочник, не как обязательный повтор с нуля.

**Связь с общим планом:** это соответствует завершению практики Фазы 4 по пункту «несколько DAG с зависимостями» и **интеграции с GitLab CI** для артефактов DAG/моделей — после того, как остальные движки в стеке уже знакомы.

---

## Обновление трекинга

После закрытия разделов чеклиста обновите таблицу **«Трекинг прогресса»** в плане практики и компетенции в [`резюме-черновик.md`](./резюме-черновик.md) по [`правила-ведения-плана-и-резюме.md`](./правила-ведения-плана-и-резюме.md).
