# План практики: инфраструктура + темы Senior DevOps / DataOps
<!-- markdownlint-disable MD024 MD036 MD060 MD026 -->

Документ задаёт **пошаговый маршрут**: что поднимать руками, что закреплять теорией и как параллельно готовить **резюме** и **собеседование**. Теория и формулировки для интервью дополняются файлом [`подготовка-к-собеседованию-dataops.md`](./подготовка-к-собеседованию-dataops.md).

Черновик резюме и правила ведения: [`резюме-черновик.md`](./резюме-черновик.md), [`правила-ведения-плана-и-резюме.md`](./правила-ведения-плана-и-резюме.md).

**Карта курса (одна страница):** [`карта-курса-лаборатории.md`](./карта-курса-лаборатории.md) — что пройдено и что дальше.

---

## Профиль лаборатории (зафиксировано)

| Параметр | Значение |
| --- | --- |
| Железо хоста | 16 vCPU, 64 GB RAM, 1 TB SSD |
| Виртуализация | VirtualBox |
| Фокус индустрии | Финтех (комплаенс, секреты, аудит, надёжность — в рамках учебного контура) |
| Текущий коммерческий бэкграунд | Senior DevOps, техлид; Docker, Ansible, Jenkins (Groovy); **цель лаборатории — закрыть пробелы: Kubernetes, GitLab CI** |
| Объём стека | **Полный**: Kubernetes, GitLab CI, Airflow, dbt, **ClickHouse и Trino**, MinIO/S3-совместимое хранилище, observability |

**Связка с текущим опытом для истории на интервью:** вы не «учите DevOps с нуля», а переносите инженерную зрелость (Jenkins pipelines → GitLab CI/CD, автоматизация через Ansible → декларативная IaC + GitOps-подход в Kubernetes). Лаборатория документирует освоение конкретного стека под Data Platform.

---

## Текущий статус (обновлено)

### Сделано

- `vm-gitlab` доступен по `192.168.1.102`.
- GitLab Runner поднят и привязан к GitLab на `192.168.1.102`.
- `vm-k8s` доступен по `192.168.1.112`.
- Kubernetes поднят на `vm-k8s` (выбран вариант `k3s`).
- В `platform-infra` создан Python data-service (CSV/API -> Parquet/JSON, `/health`) и Dockerfile.
- Образ собирается в GitLab CI и публикуется в GitLab Container Registry проекта.
- Сервис задеплоен в k3s через Helm (`deploy_helm_k3s`), `/health` отвечает.
- Ingress для `data-service` развернут, сервис доступен по `data-service.lab.local` (рабочий endpoint: `/health`).
- В GitLab CI добавлен post-deploy smoke-check (`smoke_check_k3s`) после Helm деплоя; сборка/деплой проходят.
- В `platform-infra` реализовано разделение окружений `dev/staging` (отдельные Helm values и deploy job в CI).
- Для ingress-имен `data-service-dev.lab.local` и `data-service-staging.lab.local` добавлены alias в `etc_hosts_aliases` через Ansible inventory.
- Пайплайн `dev -> staging` отработал успешно: `deploy_helm_dev`/`smoke_check_dev` и ручной `deploy_helm_staging`, сервисы в кластере обновлены.
- Добавлен runbook `dev -> staging` с шагами проверки и rollback (`docs/runbook-dev-staging.md` в `platform-infra`).
- В `platform-infra/docs` оформлены документы Фазы 2: `iac-env-conventions.md`, `secrets-policy.md`, `ci-variable-template.md`.
- OpenTofu skeleton подготовлен, но практический запуск **отложен** (закрыт доступ к registry провайдеров в текущем контуре).
- В MR-шаблон `platform-infra` добавлен обязательный gate-чеклист `dev -> staging` и ссылки на стандарты эксплуатации/секретов/IaC.
- Конвенции Фазы 2 применены к новому сервису `data-profile-service` в отдельном репозитории `platform-service` (отдельный chart, `dev/staging`, smoke-check через Ingress host-based).
- Оба сервиса (`data-service` и `data-profile-service`) успешно задеплоены через GitLab CI в k3s по единому процессу `build -> deploy_dev -> smoke -> deploy_staging`.
- В `platform-service` добавлен обязательный CI gate `validate` (Python compile + `helm lint`) перед сборкой и деплоем.
- Для `platform-service` внедрены API/контрактные тесты FastAPI в CI (`validate_api_contract`) до `build`, полный pipeline проходит успешно.
- Для `platform-service` внедрён отдельный `data_quality_gate_dev` (schema/null/range checks) перед `deploy_helm_staging`; полный pipeline успешно отрабатывает с новым quality gate.
- В `platform-service` выполнен рефактор CI deploy-слоя: логика из `ci/includes/deploy.yml` вынесена в `ci/scripts/*.sh` (без изменения поведения pipeline).
- В `platform-service` добавлен документ-стандарт `docs/ci-architecture.md` (структура `ci/includes + ci/scripts`, onboarding нового сервиса и добавление новых gates).
- Для `platform-service` введён централизованный policy-файл `data-profile-service/quality-policy.json`; data-quality gate читает пороги (required columns, null-rate, numeric ranges) из конфигурации.
- В `platform-service` добавлен `docs/staging-quality-checklist.md`: ручной go/no-go чеклист перед `deploy_helm_staging` и шаги rollback.
- Для `platform-service` зафиксированы лабораторные SLA/SLO и error budget в `docs/slo-sla.md` с привязкой к CI quality gates.
- Для Фазы 4 добавлен архитектурный baseline `platform-service/docs/data-path-minimum.md` (MinIO в k3s, Airflow/dbt контур, MVP-критерии).
- В `platform-service` подготовлен bootstrap MinIO: manual CI jobs `deploy_minio_dev`/`init_minio_buckets_dev`, values-файлы `helm/minio/*` и runbook `docs/minio-bootstrap.md`.
- В `platform-service` успешно выполнен практический deploy MinIO в `data-platform-storage` и инициализированы buckets `raw/staging/marts`.
- В `platform-service` подготовлен Airflow baseline: DAG-каркас `airflow/dags/raw_to_staging.py`, `helm/airflow/values-dev.yaml`, job `validate_airflow_dags`, manual `deploy_airflow_dev`, документ `docs/airflow-baseline.md`.
- Пройден базовый трек Airflow (уроки 1–5, [`программа-обучения-airflow.md`](./программа-обучения-airflow.md)): деплой в k3s (Helm revision 13), Ingress `airflow-dev.lab.local`, DAG `raw_to_staging_minimal` (params, Variables, Connection `minio_lab`, S3Hook → MinIO), стабильные `AIRFLOW_FERNET_KEY` / `AIRFLOW_WEBSERVER_SECRET_KEY` в GitLab CI; run с `validate_raw_zone` success (`keys_found=1`).
- Пройдена программа **dbt / Trino / ClickHouse** (треки A–D, 2026-06-03): CI `validate_dbt`, `smoke_trino_lake_dev`, `publish_mart_clickhouse_dev`, DAG `promote_to_staging`; см. [`программа-обучения-dbt-clickhouse-trino.md`](./программа-обучения-dbt-clickhouse-trino.md).
- Airflow §6.1–6.5: ретраи на `validate_raw_zone`, runbook [`platform-service/docs/airflow-runbook.md`](../../platform-service/docs/airflow-runbook.md).
- MinIO Web Console через Ingress **`minio-dev.lab.local`** (Traefik, console :9001); учётные данные лаборатории в `minio-bootstrap.md` (`minio_admin` / `minio_password`).
- MVP data-path по [`platform-service/docs/data-path-minimum.md`](../../platform-service/docs/data-path-minimum.md) — критерии закрыты (2026-06-03).
- **Airflow §6.6 закрыт (2026-07-14):** DAG `raw_to_staging_minimal` — `run_dbt_transform` → `trino_smoke_lake` → `verify_mart_clickhouse` → `end`; Grid целиком success. Scheduler memory **2Gi** (OOM при 1Gi / LocalExecutor). ConfigMap `airflow-dbt-project` (`dbt-project.tar.gz`), RBAC для KPO.

### В работе (текущая точка курса)

Сводка: [`карта-курса-лаборатории.md`](./карта-курса-лаборатории.md).

- **Фаза 8 упаковка ✅** — [`программа-обучения-упаковка.md`](./программа-обучения-упаковка.md) (устно повторять STAR).
- Опц. трек H / StatsD; мок-интервью.

### Предстоит (долгий хвост)

- Устная практика STAR / мок; опц. трек H / StatsD.

---

## Лаборатория на VirtualBox (рекомендуемая топология)

У вас достаточно ресурсов для **двух ВМ** — так проще разделить «контрольную плоскость CI» и «рабочую нагрузку кластера».

### Вариант A (рекомендуется): две ВМ

| ВМ | Роль | Ориентировочно | Примечание |
| --- | --- | --- | --- |
| `vm-gitlab` | GitLab CE + registry + runner(s) | 4–8 vCPU, 16–24 GB RAM, диск 80–120 GB | Omnibus требователен к RAM; runner можно на той же ВМ на старте |
| `vm-k8s` | Базовая ОС для Kubernetes | 8–12 vCPU, 32–40 GB RAM, диск 150–250 GB | k3s или kubeadm; на ней же MinIO/Airflow через Helm при нехватке ВМ |

Сеть VirtualBox: Host-Only или NAT Network + проброс портов по необходимости; зафиксировать статические IP в `docs/network.md`.

**Вариант B:** одна мощная ВМ (12 vCPU, ~48 GB RAM) — проще backup/snapshot, но сложнее изолировать отказ CI от кластера. Имеет смысл только если не хотите возиться с двумя образами.

**Снимки (snapshots):** после каждой завершённой фазы можно делать снимок ВМ для отката; в прод-привычках это не замена IaC, но для лаборатории ускоряет эксперименты.

---

## Акценты под финтех (в учебном масштабе)

- Явная **модель секретов** (никаких секретов в репозитории; masked variables / Vault-compatible паттерн там, где уместно).
- **Аудит изменений**: MR в GitLab, описание изменений инфраструктуры, теги релизов.
- **Разделение сред** dev/stage/prod-like и запрет «случайного» деплоя в прод-подобную без gate.
- Готовность проговорить **инцидент-менеджмент**, **RTO/RPO**, **доступ по минимуму** — даже если в лаборатории упрощённые политики.

---

## 1. Цели и критерии «готовности»

### Цели

- Освоить навыки уровня Senior DevOps в контексте **платформы данных**: контейнеры, Kubernetes, CI/CD, наблюдаемость, надёжность, безопасность, стоимость и эксплуатация.
- Иметь **воспроизводимый артефакт** (репозиторий / демо), который можно показать на интервью как доказательство системного мышления.
- Подготовить **резюме** и **истории по STAR** с опорой на реальные шаги лаборатории, а не только на теорию.
- Уверенно проходить технические и поведенческие блоки собеседования Senior.

### Критерии готовности (чек перед поиском работы)

- Один «сквозной» проект: от кода и пайплайна до прод-подобного окружения с мониторингом и документированным сценарием отказа/восстановления.
- Минимум **3–4 кейса** с цифрами (даже приблизительными из лаборатории: время деплоя, MTTR в учении, снижение ручных шагов и т.д.).
- Проговорены вслух ответы по блокам из файла подготовки (особенно Data Platform, CI/CD для data, SPOF, RTO/RPO, observability, инциденты, K8s).

---

## 2. Принципы выполнения плана

1. **Сначала фундамент**, потом «красивые» сервисы: Git, IaC, CI/CD, наблюдаемость, секреты и доступы — до раздувания стека data-инструментов (см. также ваш блок «запуск платформы с нуля» в конспекте).
2. **Всё в Git**: инфраструктура и приложения описаны декларативно; изменения через MR/PR с ревью самим собой (описание «что и зачем»).
3. **Один сквозной сценарий данных**: сырые данные → оркестрация → хранение → простая витрина/запрос → SLA по свежести в мониторинге.
4. **Документировать решения**: короткие ADR (Architecture Decision Record) или раздел «Решения» в README — это и материал для интервью («почему так»).

Оценка времени ниже дана ориентировочно (**12–16 недель** при 8–12 ч/неделю). При меньшей нагрузке просто растягивайте этапы.

---

## 3. Артефакт-проект (что будет «вашим продуктом»)

Рекомендуемая цель: репозиторий `data-platform-lab` (название любое) со структурой в духе:

- `infra/` — Terraform/OpenTofu или аналог; провижининг ВМ (опционально через Ansible из вашего опыта) + облака нет — всё на VirtualBox.
- `k8s/` или `helm/` — деплой компонентов, values по окружениям.
- `.gitlab-ci.yml` + при необходимости `ci/includes/` — **основной CI/CD в лаборатории** (перенос паттернов с Jenkins Groovy на GitLab).
- `apps/` — минимальное приложение-генератор событий или ETL-скрипт, Dockerfile.
- `docs/` — runbook, схема, DR-упражнение, SLO.

На интервью вы продаёте не «я прошёл курс», а **«я собрал контур и мог объяснить компромиссы»**.

---

## 4. Фазы практики

### Фаза 0. База и дисциплина (неделя 1)

### Практика

- [x] Развернуть ВМ(ы) в **VirtualBox** (см. топологию в начале документа); базовая ОС (рекомендуется Ubuntu LTS); SSH, обновления, hostname/FQDN.
- [x] Зафиксировать сеть в `docs/network.md` (IP: `192.168.1.102`, `192.168.1.112`; см. `docs/network.md` в корне репозитория).
- [x] Установить **GitLab CE** на `vm-gitlab`.
- [x] Зарегистрировать **GitLab Runner** (shell или docker executor — зафиксировать выбор).
- [x] На `vm-k8s` установить **Kubernetes** (выбран `k3s`) и настроить `kubectl`.
- [x] Создать/дополнить группу/проект в GitLab, шаблон **MR**, защищённые ветки для `main`.

### Темы Senior

- Версионирование инфраструктуры и приложений; что такое «контракт» между командами (платформа ↔ потребители данных); аудит через Merge Request.

### Артефакт для резюме

- «Развернул учебный контур на VirtualBox: GitLab CE с CI Runner, Kubernetes-кластер, единые правила репозитория и MR».

### Связь с конспектом

- Блоки про взаимодействие с командой и «платформа как сервис».

---

### Фаза 1. Контейнеризация и «первый сервис» (недели 2–3)

### Практика

- [x] Dockerfile для data-сервиса (Python: читает CSV/API, пишет Parquet/JSON, экспонирует `/health`).
- [x] Образ в **GitLab Container Registry** проекта.
- [x] Deployment в Kubernetes через Helm: `Deployment`, `Service`, `resources`, `liveness/readiness`, `imagePullSecrets` для приватного registry.

### Темы Senior

- Слои образов, безопасность базового образа, секреты (не в образе), конфигурация через окружение.

### Артефакт для резюме

- «Контейнеризировал сервис ingestion/transform, внедрил health checks и лимиты ресурсов в Kubernetes».

### Связь с конспектом

- Вопрос про Kubernetes: probes, rollout, rollback.

---

### Фаза 2. IaC и окружения (недели 4–5)

### Практика

- Описать кластер и периферию (сеть, DNS при наличии, buckets/MinIO, права) через IaC.
- Разделение **dev / staging / prod-like** через workspaces, каталоги или отдельные state — как минимум два уровня.

### Темы Senior

- Идемпотентность, state, drift, код-ревью инфраструктуры, принцип least privilege для тех. учёток.

### Артефакт для резюме

- «Автоматизировал создание окружений IaC, разделил dev/stage и политики доступа».

### Связь с конспектом

- Запуск платформы «с нуля»: первые 30/60/90 дней → у вас мини-аналог в масштабе лаборатории.

---

### Фаза 3. CI/CD и качество (недели 6–7)

### Практика

- **GitLab CI**: stages (`validate`, `build`, `deploy`, при необходимости `test-data`); кэш; artifact образов в GitLab Container Registry.
- Явная связка с вашим опытом Jenkins: те же quality gates, но в виде `.gitlab-ci.yml`, rules/changes, environments (`dev` / `staging`).
- Pipeline: линтер → тесты → сборка образа → push в registry → деплой в dev (kubectl/helm или GitLab agent — по выбору, задокументировать).
- Продвижение в stage: manual job или protected environment.
- **dbt:** CI-проверки (`dbt compile` / `dbt test`) на MR для слоя трансформаций.

### Темы Senior

- Quality gates, rollback образа, версионирование артефактов, «что проверяем до и после деплоя».

### Артефакт для резюме

- «Спроектировал CI/CD для data-сервисов: проверки до деплоя, контролируемый rollout между окружениями».

### Связь с конспектом

- Блок «CI/CD для data-разработки».

---

### Фаза 4. Data-путь в лаборатории (недели 8–10)

**Маршрут обучения:** [`программа-обучения-airflow.md`](./программа-обучения-airflow.md) (базовый трек) → [`программа-обучения-dbt-clickhouse-trino.md`](./программа-обучения-dbt-clickhouse-trino.md) → возврат в Airflow (раздел 6).

### Практика (полный стек — обязательный для вашей цели)

- **Объектное хранилище:** MinIO в Kubernetes (или вне кластера на отдельном диске ВМ) — слои `raw/`, `staging/`, `marts/` (или аналог медальонов).
- **Оркестрация:** **Apache Airflow** (Helm), PostgreSQL как backend метаданных; несколько DAG с зависимостями, ретраями, SLA sensors/alerts по возможности.
- **Трансформации:** **dbt** (отдельный репозиторий или папка в монорепо), подключение к целевому движку, тесты и документация `dbt docs`.
- **Аналитическое хранилище / запросы:** **ClickHouse** *и* **Trino** (можно упростить объём данных, но оба компонента должны быть развёрнуты и участвовать в пайплайне — например Trino поверх Hive-metastore/MinIO и ClickHouse для витрин).
- Интеграция с GitLab CI: деплой DAG/dbt-моделей по тем же правилам, что и приложения (версионирование артефактов).

### Темы Senior

- Идемпотентность задач, партиционирование данных, стоимость хранения (lifecycle на bucket).

### Артефакт для резюме

- «Собрал end-to-end пайплайн: ingestion → хранение → трансформация; описал контракты данных и форматы».

### Связь с конспектом

- Data Platform, S3/storage, performance tuning (хотя бы на уровне «как бы подошёл к проблеме»).

---

### Фаза 5. Observability и SLO (недели 10–12)

**Маршрут обучения:** [`программа-обучения-sentry-prometheus-statsd.md`](./программа-обучения-sentry-prometheus-statsd.md) (треки StatsD → Prometheus → Sentry → сквозной observability).

### Практика

- Prometheus + Grafana (или managed аналог, если в облаке): метрики приложения и инфраструктуры.
- **StatsD:** отправка метрик из приложений, мост через statsd_exporter в Prometheus.
- **Sentry:** отлов исключений и контекст ошибок в data-сервисах (DSN в CI Variables).
- Структурированные логи (минимум JSON в stdout + сбор через стек лаборатории).
- **Data-SLI:** метрика «возраст последней успешной записи» / latency пайплайна / счётчик ошибок задачи — и дашборд + алерт.

### Темы Senior

- RED/USE, ошибки алертинга (шум vs действие), связка метрик с бизнес-риском.

### Артефакт для резюме

- «Ввёл observability для data-платформы: SLI/SLO по свежести данных, дашборды и actionable alerts».

### Связь с конспектом

- Полный блок про observability для data.

---

### Фаза 5b. Terraform / OpenTofu MVP (после трека F, ~1–2 нед. вечерами)

**Зачем:** срез `@devops_jobs_feed` 2026-08-04 — Terraform часто в JD remote Senior с верхом вилки ≥400k; Ansible в проде уже есть, нужен явный IaC-нарратив.

**Маршрут:** [`программа-обучения-terraform-opentofu.md`](./программа-обучения-terraform-opentofu.md) (T0–T5).

### Практика

- OpenTofu или Terraform CLI; один провайдер под lab (kubernetes/helm/local — без обязательного AWS).
- Модуль + tfvars; `validate`/`plan` в GitLab CI (apply — manual/локально).
- Не раздувать в Terragrunt/multi-cloud до оффера.

### Артефакт для резюме

- «Описал lab-инфраструктуру как IaC (OpenTofu/Terraform); проверки plan/validate в CI».

---

### Фаза 6. Надёжность: бэкапы, отказ, учение (недели 12–14)

### Практика

- Зафиксировать для лаборатории условные **RTO/RPO** для метаданных оркестратора и для объектного хранилища.
- Реализовать бэкап или экспорт критичного состояния (БД Airflow, если есть; или версионируемые дампы конфигов + данные в bucket).
- **DR/game day на столе:** симуляция «потеряли namespace / удалили bucket» — восстановление по runbook, замер времени.

### Темы Senior

- SPOF в вашей лаборатории: честно перечислить и что бы сделали в проде.

### Артефакт для резюме

- «Описал и отработал сценарии восстановления; привязал стратегию бэкапов к целевым RTO/RPO».

### Связь с конспектом

- SPOF, RTO/RPO, DR-тесты, инцидентный процесс.

---

### Фаза 7. Безопасность и эксплуатационная зрелость (недели 14–15)

### Практика

- RBAC в кластере, NetworkPolicy хотя бы для одного сервиса (даже простая).
- Сканирование образов (встроенное в registry или Trivy в CI).
- Политика секретов: External Secrets / Sealed Secrets / облачный секрет-менеджер — один выбранный подход.

### Темы Senior

- Threat model «на салфетке» для вашего лабораторного контура.

### Артефакт для резюме

- «Ужесточил модель доступа и поставку секретов; включил базовые проверки безопасности в CI/CD».

---

### Фаза 8. Упаковка под собеседование и резюме (неделя 16+)

### Практика

- README: диаграмма потока данных, список компонентов, как поднять за 15 минут (или скрипт `make up`).
- 3–4 истории STAR: инцидент в лаборатории, компромисс цена/надёжность, спорное архитектурное решение, обучение «как бы» коллеги через runbook.

### Темы Senior

- План первых 90 дней на новой работе; вопросы работодателю из вашего чек-листа.

### Артефакт для резюме

- Обновлённое CV с акцентом на **платформу**, **SLO**, **CI/CD**, **Kubernetes**, **наблюдаемость**, **надёжность** — в формулировках из мини-шпаргалки конспекта.

---

## 5. Параллельные треки (не откладывать на конец)

### Резюме

- После **фазы 3** уже можно добавить сильный блок про CI/CD.
- После **фазы 5** — конкретика про мониторинг и SLO.
- Каждые 2 недели: 30 минут на синхронизацию формулировок с реально сделанными коммитами.

### Собеседование

- Раз в неделя: 2 вопроса из файла подготовки — ответ вслух 60–120 секунд, записать голосом или текстом.
- Раз в 2–3 недели: мок-интервью с напарником или AI с фокусом на **глубину follow-up**.

---

## 6. Трекинг прогресса (простая таблица для копирования)

Сводный вид «сделано / в работе / дальше»: [`карта-курса-лаборатории.md`](./карта-курса-лаборатории.md).

| Фаза | Статус | Срок (план) | Сделано (дата) | Ссылка на PR/коммит | Кейс для интервью |
| --- | --- | --- | --- | --- | --- |
| 0 | Завершено | неделя 1 | 2026-05-01 | добавить | GitLab+Runner+k3s на VirtualBox |
| 1 | Завершено | недели 2-3 | 2026-05-06 | platform-infra (main) | Data-service: Docker -> Registry -> Helm deploy + Ingress + smoke-check |
| 2 | Завершено | недели 4-5 | 2026-05-06 | platform-infra + platform-service (main) | IaC-конвенции окружений и единый процесс деплоя для двух сервисов |
| 3 | Завершено | недели 6-7 | 2026-05-06 | platform-infra + platform-service (CI quality + docs + SLA/SLO) | Введены quality gates, policy-driven проверки, CI-стандарты, staging checklist и эксплуатационные SLO |
| 4 | Завершено | недели 8-10 | 2026-07-14 | platform-service | MinIO, AF 1–6, dbt A–D, Trino/CH, E2E DAG до `end` ✅; дальше — phase 5 observability |
| 5 | В работе | недели 10-12 | 2026-08-12 | platform-service (Prometheus) | Трек F ✅; E/G опционально |
| 5b | Завершено | после F | 2026-08-13 | platform-infra/tofu + ci/includes/tofu.yml | OpenTofu MVP: import, модуль, CI validate/plan |
| 6 | Завершено | недели 12-14 | 2026-08-13 | platform-service/docs/dr-baseline.md + scripts/dr | RTO/RPO; backup PG/MinIO; game day 23s |
| 7 | Завершено | недели 14-15 | 2026-08-13 | platform-service security-baseline + NP/SA + Trivy CI | S0–S3: NP+SA+Trivy+secrets policy+резюме |
| 8 | Завершено | неделя 16+ | 2026-08-13 | README + docs/interview-packaging.md | README/диаграмма; 4 STAR; 90д; pitch |

---

## 7. Ответы на вводные (зафиксировано)

1. **Лаборатория:** рабочий ПК **16 vCPU / 64 GB RAM / 1 TB SSD**; виртуальные машины **VirtualBox** (топология — в начале файла).
2. **Индустрия:** **финтех** — в плане усилены безопасность, аудит изменений, секреты; на интервью готовность обсуждать комплаенс и разделение доступов на концептуальном уровне.
3. **Текущий уровень:** **Senior DevOps, техлид**; в проде: Docker, Ansible, Jenkins (Groovy). Лаборатория закрывает пробелы: **Kubernetes**, **GitLab CI**, оркестрация и observability для **Data Platform**.
4. **Стек:** **полный** — Kubernetes, GitLab CI, Airflow, dbt, ClickHouse, Trino, S3-совместимое хранилище, мониторинг — чтобы опираться на реально поднятый контур.

При смене топологии (например, runner переехал в Kubernetes) обновите этот раздел и `docs/network.md` в репозитории лаборатории.

---

## 8. Связь документов

| Документ | Назначение |
| --- | --- |
| Этот план | Что делать по неделям, какая инфраструктура, какие артефакты |
| [`подготовка-к-собеседованию-dataops.md`](./подготовка-к-собеседованию-dataops.md) | Как формулировать ответы на интервью |
| [`резюме-черновик.md`](./резюме-черновик.md) | Наращиваемый черновик резюме по мере фаз |
| [`правила-ведения-плана-и-резюме.md`](./правила-ведения-плана-и-резюме.md) | Дисциплина прохождения плана и синхронизации с резюме |

Рекомендуется позже завести `кейсы-star.md` под истории для интервью.
